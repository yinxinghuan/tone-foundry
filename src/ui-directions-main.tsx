import React from 'react'
import ReactDOM from 'react-dom/client'
import { UIDirectionsPage } from './ToneFoundry/UIDirectionsPage'
import './index.less'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <UIDirectionsPage />
  </React.StrictMode>,
)
